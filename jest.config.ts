// external imports
import type { Config } from 'jest';

// internal imports

// implementation
const config: Config = {
    verbose: true,
    roots: [
        './tests/'
    ]
};

// exports
export default config;